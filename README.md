# scratch-gui (PineEditor GUI) 🍍

PineEditor 的 GUI 源码，基于 **Bilup**（MistWarp / TurboWarp 派生的性能版 Scratch mod）
并深度魔改自 **RemixWarp**。本仓库结构与 RemixWarp`/`TurboWarp` 的 scratch-gui 保持一致。

核心魔改见 [PineEditor 总览仓库](https://github.com/PYC5635/PineEditor)。

> `node_modules`、`dist`、`build` 等构建产物已排除；依赖安装后即可构建。